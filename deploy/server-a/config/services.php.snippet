<?php
// Fragment for Server A's config/services.php — add this array under the
// existing 'services' config, alongside stripe/paypal/etc.
//
// The app_url is where Server A sends every /api/v1/auth/* call and where it
// redirects users to complete the SSO handoff after a successful login.

return [
    // ...

    'wavadesk' => [
        'app_url' => env('WAVADESK_APP_URL', 'https://app.wavadesk.com'),
    ],

    // ...
];
